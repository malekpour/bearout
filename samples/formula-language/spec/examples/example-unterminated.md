+++
schema = "example/formula-language/example@1"
id = "example-unterminated"
source = '="abc'
expected_error = "error-unterminated-string"
exercises = ["production-primary"]

+++

# Example `unterminated`

The string never closes.
