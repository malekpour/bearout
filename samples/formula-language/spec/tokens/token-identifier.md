+++
schema = "example/formula-language/token@1"
id = "token-identifier"
kind = "identifier"
variant = "Identifier"
pattern = "[A-Za-z_][A-Za-z0-9_.]*"
chapter = "chapter-functions"
+++

# Token `identifier`

A name. Only valid as a function name; a bare identifier is an error.
