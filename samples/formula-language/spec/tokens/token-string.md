+++
schema = "example/formula-language/token@1"
id = "token-string"
kind = "literal"
variant = "Str"
pattern = '"([^"]|"")*"'
chapter = "chapter-values"
+++

# Token `string`

A string literal; `""` inside the quotes stands for one `"`.
