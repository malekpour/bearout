+++
schema = "example/formula-language/production@1"
id = "production-reference"
rule = "token-cell [ token-colon token-cell ]"
uses = ["token-cell", "token-colon"]
chapter = "chapter-values"

+++

# Production `reference`

A single cell, or a range between two cells.
