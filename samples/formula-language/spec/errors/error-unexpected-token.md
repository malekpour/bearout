+++
schema = "example/formula-language/error@1"
id = "error-unexpected-token"
variant = "UnexpectedToken"
chapter = "chapter-errors"
+++

# Error `UnexpectedToken`

## Summary

A token appears where the grammar allows none of its kind, including a
bare identifier that is not followed by `(`, and trailing input after a
complete formula.
